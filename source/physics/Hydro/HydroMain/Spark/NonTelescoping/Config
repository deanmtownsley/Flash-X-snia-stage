##python3:genLines

# Config file for the Spark Hydro solvers without telescoping
# [physics/Hydro/HydroMain/Spark/NonTelescoping]

import os.path
import globals
from globals import *

def genLines(setupvars):

    yield '''

UNOFFICIAL

USESETUPVARS useMP5
USESETUPVARS useGLM
USESETUPVARS useTVD
USESETUPVARS useFog
USESETUPVARS useLim03
USESETUPVARS nontelescoping

# The Amrex Grid does not support SCRATCH_CTR variables in the way needed here.
# However, instead of generating failure ar sewtup time by using something like
#   CONFLICTS Grid/GridMain/AMR/Amrex
# here, let is just define a preprocessor symbol here that allows to deal with
# the situation at run time.
USESETUPVARS Grid
IF Grid in ["","Amrex","Milhoja"]
  PPDEFINE CONFLICT_AMREX_AND_SPARKNT
#ENDIF

IF useTVD or useLim03 or useFog
  GUARDCELLS 2
	PPDEFINE NSTENCIL 2
ELSE
  GUARDCELLS 3
	PPDEFINE NSTENCIL 3
ENDIF

# Variables required for state saving
SCRATCHCENTERVAR mass 
SCRATCHCENTERVAR xmom 
SCRATCHCENTERVAR ymom 
SCRATCHCENTERVAR zmom 
SCRATCHCENTERVAR mene                  	

PARAMETER telescoping          BOOLEAN  FALSE
'''

    for i in range(33-5):
        yield 'SCRATCHCENTERVAR zz%.2d' % (i+6)

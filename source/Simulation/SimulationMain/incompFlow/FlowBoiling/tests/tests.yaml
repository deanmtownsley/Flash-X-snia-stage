# YAML file for FlowBoiling tests
# comments can be added with "#"

Comparison/incompFlow/FlowBoiling/2d/AMReX:
  setupOptions: -auto -maxblocks=500 -2d -nxb=16 -nyb=16 +amrex +parallelIO -site=$SiteHome +incomp -tomlfile=$JobWorkDir/job.input
  tomlfiles: flash.toml



